

var avgTemp : tempMax, avgHumi : humiMax


{
            "avg_temp": 10,
    "tempMax": 50,
    "avg_humi": 10,
    "humiMax": 50
}

            if(avg_temp <tempMax){ 10<50=true
                on
            }else{
              mati
            }

              if(avg_humi < humiMax){ 10<1=false
                on
            }else{
               mati
            }

           avg_temp: avgRows[0].avg_temp,
           tempMax:rows[0].tempMax,
           avg_humi:avgRows[0].avg_humi,
           humiMax:rows[0].humiMax,